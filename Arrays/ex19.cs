using System;
using System.Xml.Schema;

namespace prog
{
    class Arrays
    {
        static void Main()
        {
            const int SIZE = 10;

            int[] age = new int[SIZE];
            char[] gender = new char[SIZE];
            double[] salary = new double[SIZE];
            bool[] becary = new bool[SIZE];
        }
    }
}